{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/medguardian",
            "envFile": "${workspaceFolder}/.env.local",
            "cwd": "${workspaceFolder}"
        }
    ]
}
